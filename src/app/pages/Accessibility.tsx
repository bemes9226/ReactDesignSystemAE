import * as React from 'react';

interface IAccessibilityProps {
}

const Accessibility: React.FunctionComponent<IAccessibilityProps> = (props) => {
  return (
      <h3>Accessibility Page</h3>
  );
};

export default Accessibility;
